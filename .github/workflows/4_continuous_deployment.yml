name: 4 - Continuous Deployment
run-name: "${{ github.workflow }}"

permissions: write-all

on:
  # Runs when the previous stage finished
  workflow_run:
    workflows: ["3 - Continuous Staging"]
    types: [completed]

  workflow_dispatch:

env:
  REGISTRY: ${{ vars.REGISTRY }}
  MLFLOW_TRACKING_URI: ${{ vars.MLFLOW_TRACKING_URI }}
  MLFLOW_EXPERIMENT_NAME: ${{ vars.MLFLOW_EXPERIMENT_NAME }}
  MLFLOW_MODEL_NAME: ${{ vars.MLFLOW_MODEL_NAME }}

jobs:
  deploy-prod:
    # Run only if previous stage finished successfully
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      # Here we are just loging in to GHCR and pulling the serving image with production tag and running it
      # In a real setup the deployment can be made in several ways, depending on the automation,
      # purpose, and infra. Some examples:
      # - We can connect through ssh and update the env
      # - if we have orchestrators we can send/define/update the new versions to where the system is watching
      # - the system itself might be monitoring repos and update when the new versions are pushed
      # Also, in real setups the infra will be decoupled from the models. Here everything moves forward together
      # but if we only change the model it is not necessary to redeploy the infrascruture. Serving frameworks
      # such as TF serving can manage model A/B testing, rollout, rollback, ...
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - run: docker pull ${{ env.REGISTRY }}/${{ github.repository }}/serving:production

      - name: Create OVPN file
        run: |
          echo "${{ secrets.OVPN_CONFIG_FILE }}" > DEI.ovpn
          
          # --- Split Tunneling Fix ---
          # 1. Ignore the server's directive to route ALL traffic through the VPN
          echo "pull-filter ignore \"redirect-gateway\"" >> DEI.ovpn
          
          # 2. Manually route the private networks through the VPN
          # Route 10.0.0.0/8 (covers MLflow and internal VMs if they are in this range)
          echo "route 10.0.0.0 255.0.0.0" >> DEI.ovpn
          
          # 3. Explicitly route the Production VM host
          echo "route ${{ secrets.SERVING_VM_HOST }} 255.255.255.255" >> DEI.ovpn

      - name: Connect to VPN
        uses: "kota65535/github-openvpn-connect-action@v3"
        with:
          config_file: DEI.ovpn
          username: ${{ secrets.OVPN_USERNAME }}
          password: ${{ secrets.OVPN_PASSWORD }}

      - name: Ping test
        run: |
          set -x
          ping -c 4 ${{ secrets.SERVING_VM_HOST }} || echo "ping failed with code $?"

      # ===================== Setup serving container =====================
      - name: Save serving image to tar
        run: docker save ${{ env.REGISTRY }}/${{ github.repository }}/serving:production -o /tmp/serving_production.tar

      - name: Copy serving image to production VM
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVING_VM_HOST }}
          username: ${{ secrets.SERVING_VM_USERNAME }}
          password: ${{ secrets.SERVING_VM_PASSWORD }}
          source: /tmp/serving_production.tar
          target: ~/docker_images
          strip_components: 1

      - name: Connect to DEI server via SSH and prepare environment
        uses: appleboy/ssh-action@v1.2.4
        with:
          host: ${{ secrets.SERVING_VM_HOST }}
          username: ${{ secrets.SERVING_VM_USERNAME }}
          password: ${{ secrets.SERVING_VM_PASSWORD }}
          script: |
            # Stop and remove existing serving container if running
            docker rm -f serving-app-production || true
            # Load the serving image from the transferred tar file
            docker load --input ~/docker_images/serving_production.tar

      - name: Run production on remote server
        env:
          MLFLOW_TRACKING_URI: ${{ env.MLFLOW_TRACKING_URI }}
          MODEL_ALIAS: ${{ github.sha }}
          MLFLOW_MODEL_NAME: ${{ env.MLFLOW_MODEL_NAME }}
        uses: appleboy/ssh-action@v1.2.4
        with:
          host: ${{ secrets.SERVING_VM_HOST }}
          username: ${{ secrets.SERVING_VM_USERNAME }}
          password: ${{ secrets.SERVING_VM_PASSWORD }}
          envs: MLFLOW_TRACKING_URI,MODEL_ALIAS,MLFLOW_MODEL_NAME
          script: |
            docker run -d \
              --name serving-app-production \
              -p 9001:8080 \
              -e MLFLOW_TRACKING_URI \
              -e MODEL_ALIAS \
              -e MLFLOW_MODEL_NAME \
              ${{ env.REGISTRY }}/${{ github.repository }}/serving:production
